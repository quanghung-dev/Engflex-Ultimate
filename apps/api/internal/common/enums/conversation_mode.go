package enums

// ConversationMode selects free talk vs roleplay (conversations.mode).
type ConversationMode string

const (
	ConversationModeFreeTalk ConversationMode = "free_talk"
	ConversationModeRoleplay ConversationMode = "roleplay"
)
