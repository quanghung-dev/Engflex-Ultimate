/** mock-only consts: dashboard-only references into other features' fixtures. */
export const ACTIVE_LESSON_ID = "negotiating-project-timelines";

/** The dashboard mock's bespoke titles differ from the contract scenario titles — accepted (§9.1). */
export const VOICE_PARTNER_SCENARIO_IDS = [
	"scenario-01",
	"scenario-02",
] as const;

export const HIGH_FRICTION_COUNT = 4;
